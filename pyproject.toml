[project]
name = "infoblox-mass-import-av"
version = "0.1.0"
description = "Web UI for Cal Poly AV Infoblox migrations and DHCP host reservations"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115",
  "uvicorn[standard]>=0.30",
  "python-multipart>=0.0.9",
  "jinja2>=3.1",
  "requests>=2.32",
  "python-dotenv>=1.0",
]

[project.optional-dependencies]
dev = ["pytest>=8.