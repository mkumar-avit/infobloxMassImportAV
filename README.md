# infobloxMassImportAV

Generate Infoblox NIOS CSV imports for DNS Host Records with IPv4 Host Addresses configured for DHCP.

The project is initialized here so feature work can be proposed through pull requests.
